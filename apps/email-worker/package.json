{
  "name": "email-worker",
  "module": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:worker": "bun build --bun ./index.ts --outdir ./dist --target=bun",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "18.7.9",
    "wrangler": "^4.78.0"
  },
  "dependencies": {
    "@salora/database": "workspace:*",
    "@salora/mailer": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "nodemailer": "^6.10.0",
    "typescript": "^5.8.2"
  }
}
